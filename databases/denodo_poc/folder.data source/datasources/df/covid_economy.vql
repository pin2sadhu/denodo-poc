# REQUIRES-PROPERTIES-FILE - # Do not remove this comment!
CREATE OR REPLACE DATASOURCE DF covid_economy
    FOLDER = '/data source'
    ROUTE LOCAL 'LocalConnection' '${databases.denodo_poc.folder.data source.datasources.df.covid_economy.ROUTE.LOCAL.PATH}'
    COLUMNDELIMITER = ','
    ENDOFLINEDELIMITER = '\n'
    HEADER = TRUE;

